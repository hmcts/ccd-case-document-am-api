spring:
  cloud:
    propertiesvolume:
      paths: /mnt/secrets/s2s,/mnt/secrets/ccd
      prefixed: false
      aliases:
        s2s-secret: idam.s2s-auth.totp_secret
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        microservicekey-ccd-case-document-am-api: CCD_DOCUMENT_API_IDAM_KEY
